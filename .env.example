# Environment Configuration Template
# Copy this file to .env and update the values as needed

# ===========================================
# VITE BUILT-IN CONSTANTS (KHÔNG CẦN SET)
# ===========================================
# Những biến này được Vite tự động tạo, KHÔNG cần định nghĩa trong .env:
# - import.meta.env.DEV (boolean) - true khi dev mode
# - import.meta.env.PROD (boolean) - true khi production mode  
# - import.meta.env.MODE (string) - "development" hoặc "production"
# - import.meta.env.SSR (boolean) - true khi SSR mode

# ===========================================
# CUSTOM ENVIRONMENT VARIABLES (CẦN PREFIX VITE_)
# ===========================================
# Chỉ các biến có prefix VITE_ mới được expose cho client code

# App Configuration
VITE_APP_TITLE=AutoTrader
VITE_APP_VERSION=1.0.0

# API Configuration  
VITE_API_BASE_URL=http://localhost:3000/api
VITE_API_TIMEOUT=10000

# Authentication
VITE_AUTH_TOKEN_KEY=autotrader_token
VITE_AUTH_REFRESH_TOKEN_KEY=autotrader_refresh_token

# Debug & Logging
VITE_ENABLE_CONSOLE_LOG=true
VITE_SHOW_ERROR_TOAST=true

# Error Messages (có thể customize)
VITE_SESSION_EXPIRED_MESSAGE=Session expired!
VITE_INTERNAL_ERROR_MESSAGE=Internal Server Error!
VITE_CONTENT_NOT_MODIFIED_MESSAGE=Content not modified!

# ===========================================
# NODE_ENV (CHO SERVER-SIDE)
# ===========================================
# NODE_ENV được dùng cho server-side code (Node.js)
# Không ảnh hưởng đến import.meta.env.DEV/PROD trong client
NODE_ENV=development

# ===========================================
# PRODUCTION OVERRIDES
# ===========================================
# Uncomment các dòng này khi deploy production:
# NODE_ENV=production
# VITE_API_BASE_URL=https://api.autotrader.com
# VITE_ENABLE_CONSOLE_LOG=false